//MUI Imports
import { AppBar, Toolbar } from '@material-ui/core'

export default function CustomAppbar() {
    return (
        <AppBar>
            <Toolbar>
                Site Name Teste - Navigation happens exclusively on the drawer?
            </Toolbar>
        </AppBar>
    )
}